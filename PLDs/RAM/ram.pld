GAL16V8
ram

PMMO NC NC NC NC  NC  PRG MMO MIS GND
NC   NC NC NC RCE TOE ROE RWE DIR VCC

/TOE = MIS * PRG + MMO * PRG

/ROE = MMO * /MIS + PMMO * /PRG

/RWE = MIS * /MMO

/RCE = MIS + MMO + PMMO * /PRG

DIR = MMO

DESCRIPTION
INPUTS:
MIS, MMO, PROG

OUTPUTS:
RAM /OE
RAM /WE
245 /OE
245 DIR ; LOW WRITE, HIGH READ

PRG
HIGH RUN
LOW  PROGRAM
